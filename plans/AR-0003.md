# AR-0003: AR context and TUI event adapter

Implement the adapter over the strict inbound/outbound envelopes from AR-0015. Every event carries project ID, AR ID,
task revision, packet digest, contract versions, session ID, and sequence.
Reject stale revisions, wrong ARs, duplicate sequences, missing predecessor
gates, and malformed events before any adapter call.

# AR-0053: Cross-project TUI request and response contract

Adapt Guidance decision-request schema 0.2 into TUI context and decision
inputs, preserving request identity, AR revision, formal-check and quality /
evidence references, and stable candidate IDs. Project TUI responses back into
revision-bound event payloads that Coordinator can persist and AWQ can gate
without inferring identity from display labels.

# AR-0005: Decision and proposal interaction

Collect per-point approve/reject/clarify responses and user-added proposals.
Require evaluation of an added proposal before selection; preserve independent
point identity and prevent cross-point authorization. Emit typed events back to
the owning AR rather than mutating local task state.


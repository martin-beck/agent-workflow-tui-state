# AR-0051: Fixed pane geometry and responsive resize

Use explicit bounded prompt-toolkit dimensions for the document, decision,
helper, editor, and footer regions. Keep nominal pane geometry independent of
content changes, allow safe contraction on narrow terminals, and rely on the
full-screen renderer's resize/redraw path for terminal size changes.

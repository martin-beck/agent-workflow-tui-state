# AR-0008: Coordinator lifecycle adapter

Consume Coordinator's public AR/revision/event interface. Accept TUI output
only when the active revision and predecessor chain match; record accepted
events and return the next revision. Never let the TUI change task identity,
dependencies, leases, or lifecycle status directly.


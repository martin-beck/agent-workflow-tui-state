# AR-0034: Interactive scenario helper script

Provide a no-argument helper that lists the generated scenarios, asks the user
to choose one, launches the actual full-screen TUI, and reports the emitted
events. Add automated tests for selection and invocation without requiring a
real terminal.

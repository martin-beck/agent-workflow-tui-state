# AR-0020: Interactive decision controls

Bind keyboard/user input to select, reject, clarify, add-proposal, safe-exit,
and reopen actions. Validate through the session boundary and emit typed
revision-bound events back to the owning AR.


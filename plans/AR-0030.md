# AR-0030: Scenario end-to-end runner and screenshots

Replay corpus actions through the same live control mapping, assert the final
event sequence, and render deterministic screenshot artifacts for every case.

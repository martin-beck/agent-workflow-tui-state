# AR-0067: GUI batch journal and guarded lifecycle

Use the same append-only event semantics as the TUI for GUI sessions. Preserve
all point responses, user proposals, evidence requests, and safe-exit events;
do not let the latest-event convenience file replace the journal. Guard native
window close with an explicit save/cancel decision and add offscreen widget and
bridge schema tests.

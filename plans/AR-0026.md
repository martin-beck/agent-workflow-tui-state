# AR-0026: Live TUI toolkit foundation

Choose and pin the smallest fitting Python toolkit for live pane rendering and
input IO. Prompt_toolkit is currently available locally; Textual remains an
alternative if its widget model is needed. Implement the event loop, headless
test harness, resize/exit handling, and fallback. Keep all AR/TUI contracts
toolkit-neutral.


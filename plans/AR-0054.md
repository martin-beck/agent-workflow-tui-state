# AR-0054: AR structure graph document and highlight projection

Make structure-graph nodes the authoritative source for design/work-plan
Markdown and named anchors. Require each decision to reference an existing node
and anchor whose text occurs in its source document; fail closed on mismatches,
and pass document-specific targets into the live renderer.

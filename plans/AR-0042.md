# AR-0042: Default live demo and anchor highlighting

Ensure the standalone executable starts with workplan/design documents,
multiple decisions, and a visible active-anchor marker so the complete UI is
usable without an external Coordinator context.

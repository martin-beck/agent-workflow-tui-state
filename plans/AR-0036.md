# AR-0036: Rich synthetic scenario inputs

Extend the synthetic TUI corpus beyond control-key coverage with semantically
distinct, privacy-safe inputs that exercise complete decision-session paths.
Every fixture carries a valid revision-bound context and bounded action trace;
validation rejects malformed identity, digest, revision, action, or expected
event data before replay. This AR owns only corpus fixtures and validation.

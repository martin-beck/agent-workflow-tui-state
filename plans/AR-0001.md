# AR-0001: TUI architecture and lifecycle contract

Define the state machine and JSON envelopes. The formal model must show the
mandatory path `AR context -> packet/formal review -> TUI -> persistence ->
Coordinator event -> AWQ check -> reconciliation/reopen -> AR continuation`.
Specify predecessor references, revisions, packet digests, session sequence,
and fail-closed predicates. Review ownership against Guidance AR-0040–0044,
Coordinator AR-0026–0029, and AWQ AR-0062–0065.


# AR-0068: Portable runtime bootstrap and environment negotiation

Define a small runtime manifest containing platform, architecture, Python ABI,
GUI/TUI capability, and archive digest. The connector may fetch or receive a
trusted archive, validates the manifest before extraction, configures its
temporary runtime path, and removes it after completion. Never execute an
untrusted or incompatible archive.

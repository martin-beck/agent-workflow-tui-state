# AR-0069: Remote GUI/TUI invocation matrix

Keep the UI rendering deterministic and headless in CI while exercising the
real connector, OpenSSH config alias, request copy, UI entry-point selection,
journal return, and Coordinator projection. Use Linux, Windows-compatible
fixtures, and architecture metadata/manifest fixtures; add containers or
emulation only where they materially increase coverage.
